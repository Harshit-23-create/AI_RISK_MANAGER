# ── Server ────────────────────────────────────
# Port the ML service listens on (cloud providers set this via $PORT)
PORT=8001

# ── LLM (optional — used by explainController if ML service calls LLM directly) ──
OPENAI_API_KEY=
GEMINI_API_KEY=
